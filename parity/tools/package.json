{
	"name": "ddd-parity-tools",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"description": "Isolated Phase-1/Phase-5 parity capture & diff toolkit for the dedede.ro Astro rebuild. NOT a project dependency — kept separate from the app's package.json so capture tooling never pollutes the shipped dependency budget.",
	"scripts": {
		"capture": "node capture-baseline.mjs",
		"serve": "node serve-static.mjs"
	},
	"dependencies": {
		"@axe-core/playwright": "4.10.1",
		"pixelmatch": "6.0.0",
		"playwright": "1.56.0",
		"pngjs": "7.0.0"
	}
}
